#include <fstream>
using namespace std;

int main()
{
    int n, I = 0, V = 0, X = 0, L = 0, C = 0;
    while(n)
    {
        while(n >= 100)
        {
            C++
        }
    }
    return 0;
}
